let cores = ['preto', 'branco', 'azul', 'vermelho']

for (let x = 0; x<cores.length; x++){
    console.log(cores[x])
}

for(let i in cores){
    console.log(cores[i])
}

for (let cor of cores){
    console.log(cor)
}